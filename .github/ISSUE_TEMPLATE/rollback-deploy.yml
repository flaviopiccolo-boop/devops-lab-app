name: Rollback Deploy
description: Request rollback to the previous stable version for an environment
title: "[Deploy Rollback]: "
labels: ["deploy-operation", "deploy-rollback-request"]
body:
  - type: markdown
    attributes:
      value: |
        Use this form to request rollback of an environment to the previous stable release.
  - type: dropdown
    id: target_environment
    attributes:
      label: Target environment
      description: Select the environment to rollback.
      options:
        - staging
        - prod-blue
        - prod-green
    validations:
      required: true
  - type: markdown
    attributes:
      value: |
        **Available versions:** check the [Docker Hub tags](https://hub.docker.com/r/flaviopiccolo/devops-lab-app/tags) page for a list of deployed image tags.
  - type: input
    id: rollback_version
    attributes:
      label: "Rollback version (optional)"
      description: "Copy a tag from Docker Hub (e.g. sha-abc1234). Leave blank to use the previous stable version."
      placeholder: "sha-abc1234"
    validations:
      required: false
  - type: textarea
    id: incident
    attributes:
      label: Incident / rollback reason
      description: Describe the problem that requires rollback.
      placeholder: Error rate increased after deployment, rollback needed.
    validations:
      required: true
  - type: checkboxes
    id: checks
    attributes:
      label: Confirmation
      options:
        - label: I confirm rollback should target the previous stable version.
          required: true
        - label: I understand rollback may require environment approval before execution.
          required: true